# Cursor Codex by dv8

This folder packages `thermo-nuclear-code-quality-review` and `unslop` for Codex. The plugin identifier remains `cursor-codex@dv8-marketplace`.

Maintain the Codex adaptations here on the `dv8/main` branch of [DenislavVelichkov/plugins](https://github.com/DenislavVelichkov/plugins/tree/dv8/main/cursor-code). The dv8 marketplace installs this folder directly from that branch.

Edit the files in `skills/`. Preserve their licenses and incorporate upstream changes selectively. See [SOURCES.md](SOURCES.md) for provenance and invocation policy. Bump the plugin manifest version when publishing changes.
